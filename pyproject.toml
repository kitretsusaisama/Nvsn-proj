[project]
name = "nvsn-framework"
version = "0.1.0"
description = "N-vs-N Agent Intelligence Framework"
authors = [
    { name = "Jules", email = "jules@example.com" }
]
dependencies = [
    "pydantic>=2.0",
    "pydantic-settings>=2.0",
    "structlog>=23.0",
    "httpx>=0.24",
    "numpy>=1.24",
]
requires-python = ">=3.9"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/nvsn"]

[tool.mypy]
python_version = "3.9"
strict = true
ignore_missing_imports = true

[tool.ruff]
line-length = 88
target-version = "py39"
select = ["E", "F", "I", "UP", "N", "B", "A", "C4", "PT", "RUF"]
ignore = []

[tool.pytest.ini_options]
addopts = "-ra -q"
testpaths = [
    "tests",
]
